"""
AI utilities package — Phase 2.

Modules:
  resume_extractor  — Extract text from PDF/DOCX
  resume_parser     — Parse resume text → structured JSON via Gemini
  matcher           — Score & analyze resume vs JD
  s3_uploader       — Upload files to AWS S3
"""
